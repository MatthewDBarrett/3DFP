<div class="relative w-full h-64 bg-slate-950 rounded-2xl overflow-hidden border border-slate-800">
  <div class="absolute inset-0 blueprint-grid"></div>
  
  <div class="absolute inset-0 scanning-beam"></div>

  <div class="relative z-10 flex items-center justify-center h-full">
    <div class="text-center">
      <p class="text-blue-500 font-mono text-xs uppercase tracking-[0.3em] mb-2 animate-pulse">System: Active</p>
      <p class="text-slate-500 font-mono text-[10px] uppercase">1:100 Scale Precision Grid</p>
    </div>
  </div>
</div>

<style>
  .blueprint-grid {
    background-color: #020617; /* Deep Navy Base */
    background-image: 
      linear-gradient(to right, rgba(30, 64, 175, 0.3) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(30, 64, 175, 0.3) 1px, transparent 1px);
    background-size: 40px 40px;
    mask-image: radial-gradient(circle, black 40%, transparent 90%);
  }

  .scanning-beam {
    background: linear-gradient(
      to bottom,
      transparent 0%,
      rgba(59, 130, 246, 0.1) 50%,
      rgba(59, 130, 246, 0.2) 50.5%,
      transparent 51%
    );
    background-size: 100% 200%;
    animation: scan-move 8s linear infinite;
  }

  @keyframes scan-move {
    0% { background-position: 0% 100%; }
    100% { background-position: 0% -100%; }
  }
</style>